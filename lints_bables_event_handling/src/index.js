import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
//this is component in react so that must be in upper case of first alphabet
// const Meroapp=()=>{
//   return <h2>Hello Nepal</h2>
// }
// //actually is operated like this
// ReactDOM.render(<Meroapp/>,document.getElementById('root'));